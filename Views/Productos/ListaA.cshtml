@model List<Producto>


<h1>LISTA DE PRODUCTOS </h1>

@if (Model.Count == 0) {
    <div class="alert alert-warning text-center">Aún no hay productos registrados</div>
} else {
    <ol>
    @foreach (var p in Model) {
        <li class="d-flex">
            <div><img src="@p.Foto" /></div>
            <div>@p.Nombre</div>
        </li>
    }
    </ol>
}